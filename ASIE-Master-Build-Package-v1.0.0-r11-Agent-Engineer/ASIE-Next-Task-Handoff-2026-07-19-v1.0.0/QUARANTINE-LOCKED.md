# QUARANTINE LOCKED — Top-Level ASIE Next Task Handoff

Status: TOP_LEVEL_HANDOFF_ARCHIVE
Package: Repository Surgery Package R2

This top-level handoff directory is archived.

It must not be used as a current task input, current source-of-truth, or live implementation source.

Do not copy any file from this directory into:

- `backend/`
- `src/`
- `tests/`
- `registry/`
- live `docs/`

Use current EKB and current package files instead.

R3 treatment: deletion or compaction candidate after reference checks.
