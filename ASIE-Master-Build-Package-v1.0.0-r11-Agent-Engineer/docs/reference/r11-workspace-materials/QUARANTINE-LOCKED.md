# QUARANTINE LOCKED — r11 Workspace Materials

Status: ARCHIVE_LOCKED_ROOT
Package: Repository Surgery Package R2

This directory is historical provenance only.

Do not copy, import, execute, merge, or adapt files from this directory into live implementation paths.

Live implementation paths include:

- `backend/`
- `src/`
- `tests/`
- `registry/`
- live `docs/` outside `docs/reference/`

Any implementation-looking file under this directory is non-authoritative and may be stale.

Use the current EKB and live package files instead.
