# QUARANTINE LOCKED — ASIE Architecture Correction Archive v1.0.0

Status: DANGEROUS_DUPLICATE_BUNDLE
Package: Repository Surgery Package R2

This bundle is archived historical material.

It may contain implementation-looking files, architecture maps, manifests, and plans that are older than the current EKB and live package state.

Do not copy any file from this bundle into live runtime paths.

Current source of truth:

- EKB documents under `docs/EKB/`
- live runtime files outside `docs/reference/`
- AAS Runtime Freeze Manifest
- current registry files in the live package

R3 treatment: deletion or compaction candidate after reference checks.
